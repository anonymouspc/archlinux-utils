pkgname=gnome-pkgbuild
pkgver=1
pkgrel=1
pkgdesc=gnome-pkgbuildd
arch=(any)
license=(GPL)
url=https://github.com/anonymouspc/arch
depends=($(pacman -Sg gnome | awk '{print $2}') gnome-shell-extensions)

package() { 
    : 
}
